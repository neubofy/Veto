import re

with open("app/src/main/java/com/neubofy/veto/ui/settings/AccountActivity.kt", "r") as f:
    content = f.read()

# Needs to add btnDeleteData, btnDeleteAccount
# Needs imports: URL, HttpURLConnection, JSONObject, MaterialAlertDialogBuilder
